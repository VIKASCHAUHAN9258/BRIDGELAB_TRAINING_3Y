# Assignment 6 - CSS Flexbox and Grid

Structure:
- Q1 to Q10: one folder per question
- Every question contains a separate `index.html` and `style.css`
- No JavaScript or external CSS framework is used
- Layouts use Flexbox and/or CSS Grid as required
- Responsive media queries are included where specified

GitHub suggested location:
`CSS_PRACTICE/ASSIGNMENT_6/`
